<div class="container-expand text-info">
  <h1>Tasks</h1>
</div>
{{outlet}}
{{!-- {{#each task in model}}
	<div class="well">
    <h4>{{task.title}}</h4>
    <p>{{task.description}} </p>
  </div>
  
{{/each}} --}}
